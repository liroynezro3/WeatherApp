import React from "react";
import classes from "./Header.module.css";
const Header = () => {
  return (
    <header className={classes.Header}>
      <label>by Liroy</label>
      <h1>WeatherApp</h1>
    </header>
  );
};

export default Header;

